# Gmail SMTP configuration. Copy to backend/.env and replace only the values
# on the right. Never commit the real .env file or share the app password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=khushptl173@gmail.com
SMTP_PASSWORD=bexx ggta phlt ongc
SMTP_SENDER_NAME=Carbonix
FRONTEND_URL=http://localhost:5173
